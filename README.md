#To run this project.

> Clone
> Create next project with: npm create-next-app@latest projectName
> Copy the archive node_modules to the cloned project
> in terminal: npm run dev
> open in browser the url: localhost:3000
