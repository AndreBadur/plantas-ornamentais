const FormaEnvio: React.FC = () => {
    return (
        <div className="flex justify-center items-center min-h-screen"> {/* items-center justify-center */}
            <h1 className="text-black">envio</h1>
        </div>
    );
};

export default FormaEnvio;